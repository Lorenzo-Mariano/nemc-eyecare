import { View, Text } from "react-native";
import React from "react";

export default function Service() {
	return (
		<View>
			<Text>Show the doctors that offer the service that was just shown</Text>
		</View>
	);
}
